# HITL Decisions

No human decision was required for this slice.

Architectural choice applied from existing Bahia policy: cleanup mutation remains encrypted ContextVM `worker/cleanup`; Fleet Health reads public durable state from canonical kind `30900` schemas.
