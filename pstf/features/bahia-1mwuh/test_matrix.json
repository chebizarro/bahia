{
  "feature_id": "bahia-1mwuh",
  "mappings": [
    {"criterion": "AC1", "tests": ["reloads a cached stale projection with provenance instead of rendering blank truth", "does not let an older live replay downgrade a newer cached projection", "normalizes projection truth states without collapsing absence into empty policy"]},
    {"criterion": "AC2", "tests": ["hydrates canonical relay policy and subscribes through advertised service relays", "distinguishes never-configured truth without requiring replacement confirmation", "distinguishes an explicitly signed empty policy from unavailable", "TestRelaySettingsGetPolicyDistinguishesNeverConfigured", "TestRelaySettingsGetPolicyDistinguishesStaleAndSignedEmpty"]},
    {"criterion": "AC3", "tests": ["distinguishes unavailable truth and gates Apply behind audited replacement confirmation", "TestRelaySettingsApplyRequiresExpectedProjectionHead"]},
    {"criterion": "AC4", "tests": ["includes explicit unavailable-state replacement confirmation in the encrypted request", "TestRelaySettingsApplyRecordsAuditedReplacementBeforeCanonicalMutation", "TestRelaySettingsApplyRejectsMissingOrFalseUnavailableConfirmation", "TestRelaySettingsApplyPublishesNoCanonicalStateWhenReplacementAuditFails"]},
    {"criterion": "AC5", "tests": ["migrates the legacy array shape without losing compatible relay values", "preserves compatible override values from an older object schema", "scrubs unsafe URLs from an already-current envelope", "replaces malformed persisted data with a safe versioned envelope", "never persists credential-bearing local relay URLs", "removing the final local browser relay is not overwritten by discovery fallback"]}
  ]
}
