# Verification report

The focused Go packages cover shared-release dedupe, multi-agent binding, tenant refusal, provenance conflict refusal, exact rollback lookup, API selector propagation, Nostr projection fidelity, and embedded upgrade/downgrade migration structure.

No identity/key, ACL/custody, deployment intent, desired-state host, or registry-push path is changed.
