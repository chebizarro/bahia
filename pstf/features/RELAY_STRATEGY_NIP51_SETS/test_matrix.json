{
  "feature_id": "RELAY_STRATEGY_NIP51_SETS",
  "tests": [
    {
      "test": "TestProjectorPublishesSystemDiscoverySnapshot",
      "criteria": ["NIP51-SETS-AC-001", "NIP51-SETS-AC-002"],
      "type": "unit",
      "notes": "Asserts service-key signature/pubkey, kind 30002 relay sets, d/title/relay tags, and distinct browser/contextvm/service relay source selection."
    },
    {
      "test": "TestProjectorSystemDiscoveryFailsWhenSidecarBrowserRelaysAbsent",
      "criteria": ["NIP51-SETS-AC-003"],
      "type": "unit",
      "notes": "Asserts sidecar-enabled discovery returns a deterministic nostr.browser_relays error and publishes no discovery or relay-set events."
    },
    {
      "test": "TestProjectorSystemDiscoverySurfacesRelaySetPublishFailure",
      "criteria": ["NIP51-SETS-AC-004"],
      "type": "unit",
      "notes": "Injects a relay-set publisher error shaped like an aggregate OK rejection and verifies it is returned to the snapshot caller."
    },
    {
      "test": "TestProjectorSystemDiscoveryFailsWhenRelaySetHasNoAcceptedRelays",
      "criteria": ["NIP51-SETS-AC-005"],
      "type": "unit",
      "notes": "Injects a zero-accepted publish result and verifies it is returned as a discovery projection failure instead of success."
    }
  ]
}
