# HITL decisions

The authoritative Arcana contract requires immutable digest selection. The normal path therefore trusts only configured HiveCI publishers plus an embedded OCI layout or configured registry manifest response. The browser recovery action carries only the Bahia build ID, and manual name/version/digest entry is removed from the normal UI. Advanced signed manual registration remains server-side policy gated and cannot bypass verification or deduplication.
