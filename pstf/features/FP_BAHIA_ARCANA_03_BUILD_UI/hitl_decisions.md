# HITL decisions

The user selected the fleet Gitea-authoritative boundary: Bahia resolves the protected GitHub credential server-side to sync/mirror into Gitea, and HiveCI builds the mapped NIP-34 repository. Because that initiator plumbing is absent, this change registers the signed handler and status UI but fails closed. No direct GitHub-fetch runner was invented.
