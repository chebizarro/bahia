# Human decisions — assistant unified execution

- Owner selected Option B: retain batch plan review/editing as a supported workflow.
- Owner requires one durable executor, not two dispatchers behind an interface.
- Owner fixed workflow precedence: request, persisted session, new default, then old agentic flag mapping. Persisted state always wins over later config.
- Item 1 does not alter production execution. Items 2 and 3 share a branch and switch atomically; item 4 uses the frozen contract from a separate web worktree.
- No new kind number: kind 4903 is an immutable audit fact with required domain/type/schema. Private checkpoint state must be encrypted and not leaked through tags.
