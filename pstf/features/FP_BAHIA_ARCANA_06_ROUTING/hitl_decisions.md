# FP_BAHIA_ARCANA_06_ROUTING HITL decisions

The operator selected a provider-agnostic managed route model with one production adapter matching the fleet's authoritative topology: Cloudflare remote-managed Tunnel plus proxied DNS. Local nginx or connector configuration is never shell-edited. A second provider is deferred in `bahia-5teth`.
