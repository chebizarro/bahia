{
  "feature_id": "bahia-xqea",
  "test_runs": [
    {
      "command": "pnpm test:smoke",
      "result": "expected_fail_preflight",
      "evidence": "Exited 1 before scenario execution with DockerPreflightError: Docker daemon is not available for the e2e-agent harness... no Harness cleanup failed message was emitted."
    },
    {
      "command": "pnpm exec tsx -e \"import { assertDockerDaemonAvailable } from './harness.ts'; (async () => { await assertDockerDaemonAvailable(); })();\"",
      "result": "expected_fail_preflight",
      "evidence": "Direct helper invocation threw DockerPreflightError with Docker daemon remediation text in this Docker-unavailable environment."
    },
    {
      "command": "pnpm exec tsc --noEmit",
      "result": "pass",
      "evidence": "TypeScript compile check completed with exit code 0."
    }
  ]
}
