{
  "feature_id": "WEB_NOSTR_VALIDATION_EOSE",
  "beads": ["bahia-0zzk", "bahia-f1mu", "bahia-o93z:web", "bahia-il2j:web", "bahia-zui7"],
  "criteria": [
    {
      "id": "AC1",
      "statement": "Browser relay EVENT frames are validated before subscription callbacks receive them.",
      "tests": ["web/tests/unit/sanity.test.js: validates NIP-01 event hashes before browser consumers receive relay EVENT frames", "web/tests/unit/sanity.test.js: rejects malformed signatures, timestamps, and non-string tags at the inbound validator"]
    },
    {
      "id": "AC2",
      "statement": "Browser historical queries resolve only after all connected relays in the subscription snapshot emit EOSE.",
      "tests": ["web/tests/unit/sanity.test.js: preserves relay frame order when EVENT validation is followed immediately by EOSE", "web/tests/unit/sanity.test.js: resolves historical queries only after every connected relay sends EOSE"]
    },
    {
      "id": "AC3",
      "statement": "Timeout, abort, and relay CLOSED-incomplete paths reject with a distinct incomplete-history error and preserve partial events/relay state.",
      "tests": ["web/tests/unit/sanity.test.js: treats query timeouts as incomplete history instead of successful partial results", "web/tests/unit/sanity.test.js: treats aborts as incomplete history with typed reason metadata", "web/tests/unit/sanity.test.js: rejects relay CLOSED-incomplete queries and preserves partial events for callers", "web/tests/unit/nostr-client-parsing.test.js: rejects pending bootstrap queries when reconnect attempts are exhausted before EOSE"]
    },
    {
      "id": "AC5",
      "statement": "Browser historical queries keep scoped REQ subscriptions active across relay transport reconnects and complete only after the reissued subscription receives EOSE.",
      "tests": ["web/tests/unit/sanity.test.js: keeps EOSE queries active across transport reconnect and resolves after reissued REQ EOSE", "web/tests/unit/nostr-client-parsing.test.js: keeps pending bootstrap queries subscribed when relay transport closes before EOSE"]
    },
    {
      "id": "AC4",
      "statement": "Branch and control-plane callers surface incomplete history/relay closure explicitly instead of treating it as empty success.",
      "tests": ["web/tests/unit/test-utils-and-fixtures.test.js: returns degraded metadata with partial repo state when branch history is incomplete before EOSE", "web/tests/unit/test-utils-and-fixtures.test.js: returns degraded metadata for incomplete branch history with no partial events", "web/tests/unit/controlplane-requests.test.js: awaitResult reports auth-related subscription closures distinctly", "web/tests/unit/controlplane-requests.test.js: awaitResult aggregates relay close reasons when all result subscriptions close incomplete"]
    },
    {
      "id": "AC6",
      "statement": "Partial/degraded historical reads return explicit result metadata so empty or partial data cannot be mistaken for complete EOSE history.",
      "tests": ["web/tests/unit/nostr-client-parsing.test.js: queryOrPartial returns explicit degraded metadata for aborted incomplete history", "web/tests/unit/nostr-client-parsing.test.js: fetchRuntimeCapabilities returns partial capabilities with degraded EOSE metadata when CLOSED incomplete", "web/tests/unit/nostr-client-parsing.test.js: fetchRuntimeCapabilities returns empty capabilities with degraded EOSE metadata when timeout has no partial events", "web/tests/unit/repositories-store.test.js: records degraded EOSE metadata for partial repository read models", "web/tests/unit/souls-store.test.js: loadSouls records degraded EOSE metadata from partial read models"]
    }
  ]
}
