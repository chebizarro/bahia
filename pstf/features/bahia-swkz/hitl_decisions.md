# HITL Decisions

No human decisions were required. The transition trigger was specified in the roadmap: persist units only through `POST /environments/{id}/units` or the first multi-unit config change, not ordinary deploy/apply/observe writes.
